# Local SQLite for the MVP - swap for Postgres later without changing app code
DATABASE_URL="file:./dev.db"

# Only needed once an agent calls out to a real LLM instead of the mock rules engine
# ANTHROPIC_API_KEY=
